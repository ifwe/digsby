#ifndef _ContactData_h
#define _ContactData_h

wstring groupNames(const Node* n);
void userOrderInfo(const Node* node, const wstring& fakeRootName,
                   wstring& category, wstring& key);

#endif

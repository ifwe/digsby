namespace buzz {

class XmlPrinter {

%TypeHeaderCode
#include "talk/xmllite/xmlprinter.h"
%End

public:
/*
  static void PrintXml(std::ostream * pout, const buzz::XmlElement * pelt);

  static void PrintXml(std::ostream * pout,
  const buzz::XmlElement * pelt,
    const std::string * const xmlns,
    int xmlnsCount);
*/
};

};
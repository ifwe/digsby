from msn.p12 import Switchboard as Super
class MSNP13Switchboard(Super):
#    def joi(self, socket, trid, name, nick, client_id):
#        buddy = msn.p12.conversation.joi(self, socket, trid, name, nick)
#        buddy.client_id = client_id
#
#    def iro(self, socket, trid, r_num, r_count, name, nick, client_id):
#        buddy = msn.p12.conversation.iro(self, socket, trid, name, nick)
#        buddy.client_id = client_id
    pass

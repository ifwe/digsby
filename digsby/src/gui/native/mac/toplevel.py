import gui.native

def Flash(win, titlebar = True, taskbar = True, timeout = 0, count = 3, until = 'foreground'):
    gui.native.notImplemented()
    
def FlashOnce(win):
    gui.native.notImplemented()
    
def StopFlashing(win):
    gui.native.notImplemented()
